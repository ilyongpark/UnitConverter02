[project]
name = "unitconverter02"
version = "0.1.0"
description = "Length unit converter CLI — ECB + Dual-Track TDD"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
